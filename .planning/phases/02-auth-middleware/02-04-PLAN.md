---
wave: 4
depends_on: ["03"]
files_modified:
  - server/src/Multica.Api/Middleware/RequestIdMiddleware.cs
  - server/src/Multica.Api/Middleware/ClientMetadataMiddleware.cs
  - server/src/Multica.Api/Middleware/CspMiddleware.cs
  - server/src/Multica.Api/Middleware/RequestLoggingEnricher.cs
  - server/src/Multica.Api/Program.cs
  - server/src/Multica.Api/Multica.Api.csproj
requirements:
  - MW-01
  - MW-02
  - MW-03
  - MW-04
  - MW-05
  - MW-06
  - MW-07
autonomous: true
---

## Plan 04: General Middleware Pipeline

**Objective:** Implement the remaining middleware components — request ID, client metadata, CSP, HTTP metrics, and enhanced request logging.

### Task 4.1: Request ID Middleware

<read_first>
- server/internal/middleware/request_logger.go (Go reference: chimw.GetReqID usage)
- server/src/Multica.Api/Program.cs (existing pipeline)
</read_first>

<action>
Create `server/src/Multica.Api/Middleware/RequestIdMiddleware.cs`:
- Generate UUID v7 for each request (or use `Activity.Current?.Id` / `TraceIdentifier`)
- Set `X-Request-Id` response header
- Store in `HttpContext.Items["RequestId"]` for downstream use
- If `X-Request-Id` header already present from trusted proxy, use it

Extension method: `app.UseRequestId()`
</action>

<acceptance_criteria>
- Every response includes X-Request-Id header
- Request ID is a valid UUID
- Downstream middleware can read from HttpContext.Items
- Preserves existing X-Request-Id from trusted proxies
</acceptance_criteria>

### Task 4.2: Client Metadata Middleware

<read_first>
- server/internal/middleware/client.go (Go reference: header names, context keys)
- server/src/Multica.Api/Program.cs (existing enricher)
</read_first>

<action>
Create `server/src/Multica.Api/Middleware/ClientMetadataMiddleware.cs`:
- Extract headers: `X-Client-Platform`, `X-Client-Version`, `X-Client-OS`
- Store in `HttpContext.Items["ClientPlatform"]`, etc.
- Make available via extension method: `HttpContext.GetClientMetadata() → (platform, version, os)`
- Also set on `Activity.Current` for distributed tracing

Header constants (matching Go):
```csharp
public const string HeaderClientPlatform = "X-Client-Platform";
public const string HeaderClientVersion = "X-Client-Version";
public const string HeaderClientOS = "X-Client-OS";
```

Extension method: `app.UseClientMetadata()`
</action>

<acceptance_criteria>
- Headers extracted: X-Client-Platform, X-Client-Version, X-Client-OS
- Values stored in HttpContext.Items
- Extension method returns (platform, version, os) tuple
- Missing headers return empty strings, not null
</acceptance_criteria>

### Task 4.3: CSP Middleware

<read_first>
- server/internal/middleware/csp.go (Go reference: CSP header value)
- server/src/Multica.Api/Program.cs
</read_first>

<action>
Create `server/src/Multica.Api/Middleware/CspMiddleware.cs`:
- Set `Content-Security-Policy` header on every response
- CSP value (matching Go exactly):
  ```
  default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' https: data:; connect-src 'self' wss:; frame-ancestors 'none'; object-src 'none'; base-uri 'self'; form-action 'self'
  ```

Extension method: `app.UseCsp()`
</action>

<acceptance_criteria>
- CSP header value matches Go exactly (character-by-character)
- Header present on every response
- No caching of CSP (set on every request)
</acceptance_criteria>

### Task 4.4: HTTP Metrics Middleware

<read_first>
- server/src/Multica.Api/Multica.Api.csproj (existing dependencies)
- server/src/Multica.Api/Program.cs
</read_first>

<action>
Add `prometheus-net.AspNetCore` package to `Multica.Api.csproj`:
```xml
<PackageReference Include="prometheus-net.AspNetCore" Version="8.2.1" />
```

In Program.cs, add:
```csharp
app.UseHttpMetrics(); // prometheus-net middleware
app.MapMetrics();     // /metrics endpoint
```

Metrics exposed:
- `http_requests_total` (counter, labels: method, status, endpoint)
- `http_request_duration_seconds` (histogram)
- `http_requests_in_progress` (gauge)
</action>

<acceptance_criteria>
- /metrics endpoint returns Prometheus format
- http_requests_total counter increments per request
- http_request_duration_seconds histogram records latency
- Labels include method, status code
- Build succeeds with new package reference
</acceptance_criteria>

### Task 4.5: Enhanced Request Logging

<read_first>
- server/internal/middleware/request_logger.go (Go reference: structured logging, slow-request detection, webhook path redaction)
- server/src/Multica.Api/Program.cs (existing Serilog enricher)
</read_first>

<action>
Update Serilog enricher in Program.cs to include:
- Request ID (from HttpContext.Items["RequestId"])
- User ID (from X-User-ID header)
- Client platform/version/os (from HttpContext.Items)
- Duration (already available via Serilog's diagnostic context)
- Webhook trigger ID (from HttpContext.Items, if present)

Add slow-request detection:
- Log at Warning level for requests > 1 second
- Log at Error level for requests > 5 seconds

Add webhook path redaction:
- If path starts with `/api/webhooks/autopilots/`, replace token segment with `[redacted]`
</action>

<acceptance_criteria>
- Request logs include: method, path, status, duration, request_id, user_id, client_platform/version/os
- Slow requests (>1s) logged at Warning level
- Webhook paths redacted in logs (token replaced with [redacted])
- Health endpoint (/health) skipped in logs
</acceptance_criteria>

### Task 4.6: Panic/Exception Recovery

<read_first>
- server/src/Multica.Api/Program.cs (existing ExceptionHandler)
</read_first>

<action>
Verify existing ExceptionHandler in Program.cs matches Go behavior:
- Returns 500 with `application/problem+json` content type
- Response body: `{"type": "...", "title": "Internal Server Error", "status": 500, "detail": "..."}`
- Detail only included in Development environment

If already correct, no changes needed. If not, update to match Go format.

Also add Serilog's `UseSerilogRequestLogging` error enrichment:
```csharp
options.EnrichDiagnosticContext = (diagnosticContext, httpContext) =>
{
    // ... existing enrichments
    if (httpContext.Response.StatusCode >= 500)
    {
        diagnosticContext.Set("Error", true);
    }
};
```
</action>

<acceptance_criteria>
- 500 responses return application/problem+json
- Detail field only in Development mode
- Error responses logged at Error level
- No unhandled exceptions crash the process
</acceptance_criteria>

---

## Verification Criteria

1. X-Request-Id present on all responses
2. Client metadata headers extracted and available
3. CSP header matches Go exactly
4. /metrics endpoint returns Prometheus format
5. Request logs include all required fields
6. Slow requests detected and logged
7. Webhook paths redacted in logs
8. Exception handler returns correct format
9. `dotnet build` succeeds
10. `dotnet test` passes
