---
phase: 3
plan: 06
type: execute
wave: 3
depends_on:
  - 03-01
files_modified:
  - server/src/Multica.Api/Handlers/IssueLabelHandler.cs
  - server/src/Multica.Api/Handlers/IssueMetadataHandler.cs
  - server/src/Multica.Api/Program.cs
autonomous: true
requirements:
  - ISSUE-05
  - ISSUE-06
must_haves:
  truths:
    - "User can add a label to an issue"
    - "User can remove a label from an issue"
    - "Issue detail response includes labels array"
    - "Issue list response includes labels array"
    - "User can get issue metadata as KV map"
    - "User can set a metadata key"
    - "User can delete a metadata key"
    - "Metadata keys match pattern ^[a-zA-Z_][a-zA-Z0-9_.-]{0,63}$"
    - "Metadata values must be primitives (string, number, bool)"
    - "Metadata has max 50 keys per issue"
  artifacts:
    - path: "server/src/Multica.Api/Handlers/IssueLabelHandler.cs"
      provides: "Issue label endpoints"
      contains: "AddIssueLabel, RemoveIssueLabel"
    - path: "server/src/Multica.Api/Handlers/IssueMetadataHandler.cs"
      provides: "Issue metadata endpoints"
      contains: "ListIssueMetadata, SetIssueMetadataKey, DeleteIssueMetadataKey"
  key_links:
    - from: "server/src/Multica.Api/Handlers/IssueLabelHandler.cs"
      to: "server/src/Multica.Core/Entities/IssueLabel.cs"
      via: "Many-to-many relationship"
      pattern: "IssueLabel"
    - from: "server/src/Multica.Api/Handlers/IssueMetadataHandler.cs"
      to: "server/src/Multica.Core/Entities/Issue.cs"
      via: "Metadata JSONB column"
      pattern: "Metadata"
---

## Plan 06: Issue Labels & Metadata

**Objective:** Implement issue label management and metadata KV store.

Purpose: Labels enable categorization, metadata enables agent pipeline state tracking.
Output: Working label and metadata endpoints.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-issues-comments/03-CONTEXT.md
@.planning/phases/03-issues-comments/03-01-PLAN.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From server/internal/handler/issue_reaction.go (reference pattern):
```go
type IssueReactionResponse struct {
    ID        string `json:"id"`
    IssueID   string `json:"issue_id"`
    ActorType string `json:"actor_type"`
    ActorID   string `json:"actor_id"`
    Emoji     string `json:"emoji"`
    CreatedAt string `json:"created_at"`
}
```

From server/internal/handler/issue_metadata.go (Go reference):
- Key validation: ^[a-zA-Z_][a-zA-Z0-9_.-]{0,63}$
- Max 50 keys per issue
- Values must be primitive (string, number, bool)
- JSONB column size limit: 8KB

Endpoints:
- GET /api/issues/{id}/metadata → { "metadata": {...} }
- PUT /api/issues/{id}/metadata/{key} → { "metadata": {...} }
- DELETE /api/issues/{id}/metadata/{key} → { "metadata": {...} }

From server/src/Multica.Core/Entities/IssueLabel.cs:
```csharp
// IssueLabel is a join table
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Issue Label Endpoints</name>
  <files>server/src/Multica.Api/Handlers/IssueLabelHandler.cs</files>
  <read_first>
    - server/internal/handler/issue.go (lines 119-146 for labelsByIssue bulk load)
    - server/src/Multica.Api/Handlers/IssueLabelHandler.cs (current state)
    - server/src/Multica.Core/Entities/IssueLabel.cs (join table entity)
    - server/src/Multica.Infrastructure/Data/Configurations/IssueToLabelConfiguration.cs (existing config)
  </read_first>
  <action>
Add issue label endpoints to IssueHandler.cs:

1. **LabelResponse DTO**:
   ```csharp
   public record LabelResponse(
       [property: JsonPropertyName("id")] string Id,
       [property: JsonPropertyName("workspace_id")] string WorkspaceId,
       [property: JsonPropertyName("name")] string Name,
       [property: JsonPropertyName("color")] string Color,
       [property: JsonPropertyName("created_at")] string CreatedAt,
       [property: JsonPropertyName("updated_at")] string UpdatedAt
   );
   ```

2. **AddIssueLabel Endpoint**: `POST /api/issues/{id}/labels`
   - Request body: { "label_id": "uuid" }
   - Load issue
   - Validate label exists in workspace
   - Create IssueLabel join record
   - Return 201 with label response

3. **RemoveIssueLabel Endpoint**: `DELETE /api/issues/{id}/labels/{labelId}`
   - Load issue
   - Delete IssueLabel join record
   - Return 204

4. **Bulk Load Labels** (helper method):
   - For list endpoints, load labels for all issues in one query
   - Group by issue_id
   - Attach to IssueResponse.Labels

5. **Update GetIssue to include labels**:
   - After loading issue, load labels
   - Set resp.Labels = labels (empty array if none)

6. **Update ListIssues to include labels**:
   - After loading issues, bulk load labels
   - Attach to each IssueResponse
  </action>
  <verify>
    <automated>dotnet build server/src/Multica.Api --no-restore 2>&1 | Select-String -Pattern "error" | Select-Object -First 10</automated>
  </verify>
  <acceptance_criteria>
    - POST /api/issues/{id}/labels adds label to issue
    - DELETE /api/issues/{id}/labels/{labelId} removes label
    - GET /api/issues/{id} includes labels array in response
    - GET /api/issues includes labels array for each issue
    - Labels are bulk-loaded (no N+1 queries)
    - Build succeeds without errors
  </acceptance_criteria>
  <done>Issue label endpoints working with bulk loading</done>
</task>

<task type="auto">
  <name>Task 2: Implement Issue Metadata Endpoints</name>
  <files>server/src/Multica.Api/Handlers/IssueMetadataHandler.cs</files>
  <read_first>
    - server/internal/handler/issue_metadata.go (full file: validation, CRUD)
    - server/src/Multica.Api/Handlers/AuthHandler.cs (endpoint registration pattern)
    - server/src/Multica.Core/Entities/Issue.cs (Metadata field is JsonDocument?)
  </read_first>
  <action>
Create `server/src/Multica.Api/Handlers/IssueMetadataHandler.cs`:

1. **Endpoint Registration**: `MapIssueMetadataEndpoints(this WebApplication app)`
   - Register under existing issue group or create new group

2. **ListIssueMetadata Endpoint**: `GET /api/issues/{id}/metadata`
   - Load issue
   - Parse Metadata JsonDocument to Dictionary<string, object>
   - Return { "metadata": {...} }
   - Empty object when null

3. **SetIssueMetadataKey Endpoint**: `PUT /api/issues/{id}/metadata/{key}`
   - Validate key matches pattern: ^[a-zA-Z_][a-zA-Z0-9_.-]{0,63}$
   - Validate value is primitive (string, number, bool)
   - Load issue
   - Get existing metadata
   - Check key count < 50 (or key already exists)
   - Set key in metadata
   - Update issue Metadata field
   - Return { "metadata": {...} }

4. **DeleteIssueMetadataKey Endpoint**: `DELETE /api/issues/{id}/metadata/{key}`
   - Validate key pattern
   - Load issue
   - Get existing metadata
   - Remove key
   - Update issue Metadata field
   - Return { "metadata": {...} }

5. **Validation Helpers**:
   - ValidateMetadataKey(string key): regex match
   - ValidateMetadataValue(JsonElement value): check primitive type
   - ParseMetadata(JsonDocument? doc): convert to dictionary

6. **Register in Program.cs**: Add `app.MapIssueMetadataEndpoints();`
  </action>
  <verify>
    <automated>dotnet build server/src/Multica.Api --no-restore 2>&1 | Select-String -Pattern "error" | Select-Object -First 10</automated>
  </verify>
  <acceptance_criteria>
    - GET /api/issues/{id}/metadata returns metadata object
    - PUT /api/issues/{id}/metadata/{key} sets metadata key
    - DELETE /api/issues/{id}/metadata/{key} deletes metadata key
    - Invalid key pattern returns 400
    - Non-primitive value returns 400
    - Exceeding 50 keys returns 400
    - Metadata defaults to empty object when null
    - Build succeeds without errors
  </acceptance_criteria>
  <done>Issue metadata endpoints working with proper validation</done>
</task>

</tasks>

<verification>
1. Build succeeds: `dotnet build server/src/Multica.Api --no-restore`
2. Label endpoints work correctly
3. Metadata endpoints work with validation
4. Issue responses include labels array
</verification>

<success_criteria>
- POST /api/issues/{id}/labels adds label
- DELETE /api/issues/{id}/labels/{labelId} removes label
- GET /api/issues/{id}/metadata returns metadata
- PUT /api/issues/{id}/metadata/{key} sets key
- DELETE /api/issues/{id}/metadata/{key} deletes key
- All validation rules enforced
</success_criteria>

<output>
Create `.planning/phases/03-issues-comments/03-06-SUMMARY.md` when done
</output>
